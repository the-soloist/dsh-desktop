# DSH Desktop

<p align="center">
  <img src="internal/appicon/dsh-desktop-icon.png" alt="DSH Desktop icon" width="192">
</p>

基于 Go 和 Wails 的 DeepSeek DSH 桌面客户端，支持 macOS、Windows 和 Linux，自动启动本地 DSH 服务并提供桌面窗口与托盘体验。

macOS 支持触摸板双指捏合进行视觉缩放；`Ctrl +/-`（Mac 也支持 `⌘ +/-`）调整页面元素尺寸并重新排版。两种倍率独立，键盘缩放不会重置手势缩放。
